export const config = {
  listName: "On released",
  displayText: "On released",
  description: "Triggered when the current drag ends.",
  isTrigger: true,
  isInvertible: false,
  params: [],
};

export const expose = false;

export default function () {
  return true;
}
